# Tests

The transferred Django integration suite exercises real middleware, CSRF,
authentication, ORM selections, schema browsing, table editing, source/data RPC,
transactions, ASGI dispatch and runtime assets. Django is required; these tests
must not silently skip when the integration dependency is missing.
